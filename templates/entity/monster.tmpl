// Generated from spec-kit template: entity/monster
// Monster: {{.Name}}
// Generated: {{.Timestamp}}
// Author: {{.Author}}

using LablabBean.Game.Core.Entities;
using Microsoft.Extensions.Logging;

namespace LablabBean.Game.Core.Monsters;

/// <summary>
/// {{.Description}}
/// </summary>
public class {{.Name}} : Monster
{
    public {{.Name}}(ILogger<{{.Name}}> logger) : base(logger)
    {
        // Basic properties
        Name = "{{.DisplayName}}";
        Glyph = '{{.Glyph}}';
        
        // Stats
        MaxHealth = {{.MaxHealth}};
        Health = MaxHealth;
        Attack = {{.Attack}};
        Defense = {{.Defense}};
        Speed = {{.Speed}};
        
        // AI behavior
        AiType = AIType.{{.AiType}};
        AggroRange = {{.AggroRange}};
        
        // Loot
        ExperienceValue = {{.ExperienceValue}};
        GoldDropMin = {{.GoldDropMin}};
        GoldDropMax = {{.GoldDropMax}};
    }
    
    /// <summary>
    /// Custom behavior for {{.Name}}
    /// </summary>
    public override void OnSpawn()
    {
        base.OnSpawn();
        Logger.LogDebug("{{.Name}} spawned at ({X}, {Y})", Position.X, Position.Y);
        // TODO: Add custom spawn behavior
    }
    
    /// <summary>
    /// Death behavior
    /// </summary>
    public override void OnDeath()
    {
        base.OnDeath();
        Logger.LogInformation("{{.Name}} defeated!");
        // TODO: Add custom death behavior (effects, loot, etc.)
    }
    
    /// <summary>
    /// Attack behavior
    /// </summary>
    public override int CalculateAttackDamage()
    {
        var baseDamage = base.CalculateAttackDamage();
        // TODO: Add special attack modifiers for {{.Name}}
        return baseDamage;
    }
}
